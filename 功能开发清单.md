# Wing 功能开发与优化清单

> 基于项目文档、实际代码与用户反馈梳理，最后更新：2026-01-22。本次梳理：待办一键复制到剪贴板已实现；PWA 提升为高优先级；空白引导已从待开发移除；移动端/宽屏 Tab 改造已取消。新增 11 项待办：Bug 修复 2 项、新功能 5 项、用户体验优化 4 项。今日完成：输入框失焦问题修复、时间戳传递、自定义提示语、文图排版优化。

## 📊 功能完成度概览

### ✅ 已完成功能（按模块分组）

#### 1. 记录与合成
- ✅ 碎片化记录（文本、图片 Base64）
- ✅ 消息编辑与编辑标记（`editedAt`）
- ✅ 记录页按日期展示、`?date=` 切换、日期选择器
- ✅ 日记详情「查看当天记录」跳转
- ✅ AI 智能合成（Gemini / OpenAI / DeepSeek / 自定义 Base）
- ✅ 生产环境 `/api/ai` 代理（Vercel / Netlify）
- ✅ 测试 API 连接
- ✅ 当日已有日记时「再次收拢」覆盖该日记
- ✅ 输入框无法正常输入（Bug 修复）
- ✅ 交互流程改造：AI 自动回复（发送后由猫头鹰回复并带跳转）
- ✅ AI 生成的时间戳标记和提示语支持多条，再次生成时叠加
- ✅ 输入框留空且当天已有记录时，提示长按/双击切换 AI 生成模式
- ✅ 消息气泡字号间距与 5 分钟内时间戳合并
- ✅ 空白引导提示（聊天为空时随机 3–5 个引导问题，点击填入输入框并聚焦，中英双语，`utils/promptGuides.ts`）
- ✅ 输入框失焦问题修复（滑动页面时自动失焦）
- ✅ 每天生成日记时传递消息时间戳（在 `utils/date.ts` 中创建 `formatTimestampForPrompt` 函数，格式化时间戳为「上午 10:30」「下午 3:15」格式，并在 system prompt 中说明时间戳用途）

#### 2. 日记详情
- ✅ 手动编辑标题与 Markdown
- ✅ 编辑历史（可选）、恢复旧版本
- ✅ 重新生成（合并当日全部记录，覆盖或另存为新版本）
- ✅ 单独重新生成洞察
- ✅ 生成长图、复制到剪贴板、复制为新日记、删除
- ✅ 待办一键复制到剪贴板（日记详情待办区域）
- ✅ 更多菜单聚合
- ✅ 从日记详情页重新生成时支持追加自定义提示语（重新生成对话框中添加「追加提示语」输入框）
- ✅ 日记详情内的图片排版优化（改善图片在日记详情页的显示效果，如间距、大小、对齐等）

#### 3. 数据与同步
- ✅ 导出 ZIP（`data.json` + `images/`）
- ✅ 导入 `.json` / `.zip`、替换
- ✅ 导出时可选：包含所有设置 / 仅日记与记录
- ✅ WebDAV（坚果云、测试连接、立即同步）
- ✅ WebDAV 生产代理（Netlify `/api/webdav`，规避 CORS）
- ✅ 云盘逻辑改为「备份到云盘」与「从云盘恢复」
- ✅ 实时同步开关（`realtimeWebdavSync`）
- ✅ 保留编辑历史、导出时是否备份 API Key 等选项
- ✅ IndexedDB 改造存储系统（容纳更多图片，`indexedDBStorage.ts`）

#### 4. 界面与设置
- ✅ 主题：跟随系统 / 亮色 / 暗色；黄昏与暗色（nocturnal）主题
- ✅ 猫头鹰意象：OwlLogo、EmptyStateOwl、LoadingOwl、羽毛/收拢文案
- ✅ 多语言（zh/en）、模型输出语言（与页面一致 / 中文 / 英文）
- ✅ 文风选择（书信体 / 散文体 / 报告体 / 自定义，写入合成 system prompt）
- ✅ 显示选项：页面字体（系统/思源黑体/思源宋体/霞鹜文楷）、字号大/中/小
- ✅ 根据标题层级统一字号和字重
- ✅ 聊天消息的字号同步输入框的字号（当用户调整输入框字号时，聊天消息的字号同步调整）
- ✅ 初始界面的提示语优化：改为「如果暂时没有其他想法，试试从以下问题选择一个开始记录」，避免用户以为问题是必答的
- ✅ 当光标在输入框内时，无需再出现留空提示语（优化提示逻辑，避免干扰用户输入）
- ✅ 分享链接 Open Graph（og-image、Twitter Card）
- ✅ 个人/设置页：记录热力图、今日挥动、羽毛总数
- ✅ 模型配置、存储管理
- ✅ 欢迎日记（首次空数据注入）、错误边界与 Toast

---

## 🎯 待开发功能（按优先级）

### 🔴 高优先级（剩余 2 项）

##### 1. PWA（渐进式 Web 应用） ⭐⭐⭐
**状态**：未实现  
**位置**：项目根目录（`manifest.json`、Service Worker）  
**描述**：离线可用、安装到主屏，提升移动端与独立使用体验。

**实现要点**：
- 创建 `manifest.json`（应用名称、图标、主题色）
- 实现 Service Worker（缓存策略、离线支持）
- 安装提示（「添加到主屏」）
- 离线页面提示

**技术实现**：
- 创建 `public/manifest.json`
- 创建 `public/sw.js` Service Worker
- 在 `index.html` 注册 Service Worker
- 使用 Workbox 或手动实现缓存策略
- 在 `App.tsx` 监听 `beforeinstallprompt` 事件，显示安装提示

**预计工作量**：6–8 小时

---

##### 2. Safari 浏览器传图失败修复 ⭐⭐⭐
**状态**：待修复  
**位置**：`components/ChatView.tsx`、图片上传相关代码  
**描述**：Safari 浏览器上传图片功能存在问题，需要排查并修复。

**实现要点**：
- 排查 Safari 浏览器中图片上传失败的原因
- 修复文件选择、Base64 编码等兼容性问题
- 测试并确保在 Safari 中正常工作

**技术实现**：
- 检查 `input[type="file"]` 在 Safari 中的兼容性
- 检查图片转 Base64 的逻辑
- 可能需要使用 `FileReader` API 的替代方案
- 添加 Safari 特定的错误处理

**预计工作量**：2–4 小时

---

### 🟡 中优先级（功能扩展，剩余 5 项）

#### 1. 待办多格式导出（JSON / TXT / CSV） ⭐⭐
**状态**：待实现（复制到剪贴板已实现）  
**位置**：`components/JournalDetail.tsx`（待办区域）  
**描述**：在现有「待办一键复制到剪贴板」基础上，支持将待办导出为 JSON、TXT、CSV 文件或按格式下载。

**实现要点**：
- 在待办区域标题旁添加「导出待办」按钮；按优先级筛选；多格式导出
- 创建 `utils/todoExport.ts` 处理格式转换

**预计工作量**：2–3 小时

---

#### 2. 消息记录支持天气、温度、定位 ⭐⭐
**状态**：待实现  
**位置**：`components/ChatView.tsx`、`services/weatherService.ts`  
**描述**：消息记录支持添加当天天气、温度和定位信息

**实现要点**：
- 获取用户位置（使用 Geolocation API）
- 调用天气 API（如 OpenWeatherMap、和风天气）
- 在消息记录中显示天气信息（可选，用户可开启/关闭）
- 保存位置信息到 `RawFragment.location`
- 天气信息显示在消息时间旁或消息卡片内

**技术实现**：
- 创建 `services/weatherService.ts` 封装天气 API
- 在 `types.ts` 添加 `RawFragment.weather?: { temp: number; condition: string; location?: string }`
- 在 `ChatView.tsx` 添加「添加天气」按钮或自动获取
- 使用 `navigator.geolocation.getCurrentPosition()` 获取位置
- 调用天气 API 获取当前天气
- 在消息卡片中展示天气图标和温度
- 添加设置选项：是否自动添加天气信息

**预计工作量**：5-6 小时

---

#### 3. 日记库搜索功能 ⭐⭐
**状态**：待实现  
**位置**：`components/JournalView.tsx`  
**描述**：日记库增加搜索功能，支持按标题、内容、日期搜索

**实现要点**：
- 搜索输入框（顶部或侧边栏）
- 实时搜索（输入即搜索）
- 支持关键词高亮
- 搜索结果统计
- 清空搜索按钮

**技术实现**：
- 在 `JournalView.tsx` 添加搜索输入框
- 使用 `useState` 管理搜索关键词
- 使用 `useMemo` 过滤日记列表
- 搜索逻辑：`entry.title.includes(keyword) || entry.markdownContent.includes(keyword)`
- 使用 `react-markdown` 的 `components` 属性高亮关键词
- 添加搜索历史（可选）
- 支持日期范围搜索（可选）

**预计工作量**：4-5 小时

---

#### 4. 日记导入（多平台） ⭐⭐
**状态**：未实现  
**位置**：`components/SettingsStorageView.tsx`  
**描述**：从 Day One、Journey 等导出内容导入，自动识别日期/标题、排版与命名

**实现要点**：
- 多格式支持：JSON（Day One）、TXT、MD、CSV
- 批量导入与预览
- 字段映射（日期、标题、正文、图片）
- 与现有 Wing 专用 `.json/.zip` 导入区分（新增「导入第三方日记」选项）

**技术实现**：
- 在 `SettingsStorageView.tsx` 添加「导入第三方日记」按钮
- 创建 `services/thirdPartyImport.ts` 处理不同格式解析
- Day One JSON：解析 `entries` 数组，提取 `creationDate`、`text`、`photos`
- TXT/MD：按文件名或首行识别日期，正文为内容
- CSV：按列映射（日期、标题、正文）
- 导入后转换为 `WingEntry` 格式并保存

**预计工作量**：6-8 小时

---

#### 5. 定时/后台自动同步 ⭐⭐
**状态**：未实现  
**位置**：`services/webdavService.ts`、`components/SettingsStorageView.tsx`  
**描述**：在现有「实时同步」基础上，增加定时或后台触发 WebDAV 备份

**实现要点**：
- 定时同步选项（每小时/每天/每周）
- 后台同步（使用 Service Worker 或 Web Worker）
- 同步状态提示（上次同步时间、下次同步时间）
- 失败重试机制

**技术实现**：
- 在 `AppSettings` 添加 `autoSyncInterval?: 'hourly' | 'daily' | 'weekly' | 'off'`
- 使用 `setInterval` 或 Service Worker 的 `periodicSync` API
- 在 `SettingsStorageView.tsx` 添加定时同步开关和间隔选择
- 记录上次同步时间到 localStorage
- 同步失败时记录错误并提示用户

**预计工作量**：5-6 小时

---

---

### 🟢 低优先级（剩余 4 项）

#### 1. 读图功能 ⭐
**状态**：未实现  
**位置**：`components/ChatView.tsx`、`services/imageAnalysisService.ts`  
**描述**：支持读取图片内容，提取图片中的文字或描述图片内容，便于用户记录图片信息。

**实现要点**：
- 上传图片后自动识别或提供「识别图片」按钮
- 支持 OCR 文字识别（如 Tesseract.js、百度 OCR、腾讯 OCR）
- 支持图片内容描述（调用视觉模型 API，如 GPT-4V、Gemini Vision）
- 识别结果自动添加到消息内容或作为附加信息
- 支持多图片批量识别

**技术实现**：
- 创建 `services/imageAnalysisService.ts` 封装图片分析逻辑
- 集成 OCR API 或使用客户端 OCR 库（如 Tesseract.js）
- 调用视觉模型 API 进行图片内容描述
- 在 `ChatView.tsx` 添加图片识别按钮和结果展示
- 处理识别失败、超时等异常情况

**预计工作量**：6–8 小时

---

#### 2. 录音转文字功能 ⭐
**状态**：未实现  
**位置**：`components/ChatView.tsx`、`services/audioService.ts`  
**描述**：支持录音转文字功能，方便用户通过语音快速记录内容。

**实现要点**：
- 录音按钮（在输入框旁或工具栏）
- 使用 Web Audio API 或 MediaRecorder API 录音
- 调用语音识别 API（如 Web Speech API、OpenAI Whisper API）
- 录音状态提示（录音中、转文字中）
- 转文字后自动填入输入框
- 支持暂停、取消录音

**技术实现**：
- 创建 `services/audioService.ts` 封装录音和转文字逻辑
- 使用 `navigator.mediaDevices.getUserMedia()` 获取麦克风权限
- 使用 `MediaRecorder` 录制音频
- 调用语音识别服务（Web Speech API 或第三方 API）
- 在 `ChatView.tsx` 添加录音按钮和状态显示
- 处理权限拒绝、网络错误等异常情况

**预计工作量**：8–10 小时

---

#### 3. 用户画像长期记忆（Beta 功能） ⭐
**状态**：未实现  
**位置**：`services/memoryService.ts`、`services/aiService.ts`  
**描述**：从每天日记中提取关键信息，建立用户画像的长期记忆（语义记忆、情景记忆、程序性记忆），beta 功能，为了给到用户更有温度的反馈。

**实现要点**：
- 从日记中提取关键信息（人物、事件、偏好、习惯等）
- 建立记忆库（语义记忆：事实性信息；情景记忆：特定时间地点的事件；程序性记忆：习惯和技能）
- 在生成日记时利用记忆库，提供更个性化的反馈
- 记忆库存储和管理（IndexedDB 或本地存储）
- 记忆更新机制（定期从新日记中提取并更新）

**技术实现**：
- 创建 `services/memoryService.ts` 管理记忆库
- 创建 `types.ts` 中的记忆类型定义（`SemanticMemory`、`EpisodicMemory`、`ProceduralMemory`）
- 使用 AI 从日记中提取关键信息（调用 AI API 分析日记内容）
- 在生成日记时，将相关记忆作为上下文传递给 AI
- 添加设置选项：是否启用长期记忆功能

**预计工作量**：10–12 小时

---

#### 4. 统计扩展 ⭐
**状态**：未实现  
**位置**：`components/SettingsMainView.tsx`  
**描述**：心情趋势、词云、连续记录天数（Streak）等（在现有热力图与基础统计上扩展）

**实现要点**：
- 心情趋势图（按日期展示 mood emoji 变化）
- 词云（从日记正文提取高频词）
- 连续记录天数（Streak）
- 月度/年度统计

**技术实现**：
- 在 `SettingsMainView.tsx` 添加「统计详情」入口
- 创建 `components/StatsView.tsx` 展示统计
- 使用图表库（如 `recharts` 或 `chart.js`）绘制趋势图
- 使用词云库（如 `wordcloud2.js`）生成词云
- 计算 Streak：遍历 `getActivityDateSet()`，找出最长连续天数
- 统计函数：`utils/stats.ts`

**预计工作量**：8-10 小时

---

## ❌ 已取消需求（不再开发）

以下需求因当前方案已满足而取消：**标题行右上角 Tab 实现简洁、可视的切换**，不再做底部 Tab 或侧边栏改造。

| 已取消项 | 原描述 |
|----------|--------|
| 移动端底部 Tab 栏滑动自动隐藏/显示 | 向下滑动隐藏、向上滑动显示 |
| 宽屏模式底部 Tab 栏改造为侧边栏 | 宽屏时 nav 固定于左侧垂直排列 |

---

## 🔧 代码优化建议

### 1. 性能优化
- [ ] 图片懒加载（`MarkdownRenderer` 中的图片）
- [ ] 虚拟滚动（日记列表过长时）
- [ ] 防抖/节流（搜索、日期切换）
- [ ] 代码分割（按路由懒加载组件）

### 2. 用户体验优化
- [x] ~~搜索功能~~（已提升为正式功能，见「日记库搜索功能」）
- [x] ~~聊天消息的字号同步输入框的字号~~（已完成，见「界面与设置」）
- [x] ~~初始界面的提示语优化~~（已完成，见「界面与设置」）
- [x] ~~当光标在输入框内时，无需再出现留空提示语~~（已完成，见「界面与设置」）
- [x] ~~日记详情内的图片排版优化~~（已完成，见「日记详情」）
- [ ] 键盘快捷键（`Cmd/Ctrl + S` 保存编辑、`Esc` 取消）
- [ ] 拖拽排序（日记列表、待办事项）
- [ ] 标签/分类（为日记添加标签）

### 3. 错误处理
- [ ] 网络错误重试机制
- [ ] 数据损坏恢复（备份检测与恢复）
- [ ] 更详细的错误提示（区分 API 错误、网络错误、解析错误）

### 4. 可访问性（A11y）
- [ ] ARIA 标签完善
- [ ] 键盘导航支持
- [ ] 屏幕阅读器优化
- [ ] 高对比度模式

---

## 📝 开发建议

### 优先级排序（按剩余待开发）

#### 高优先级（2 项）
1. **PWA**（高 #1）
2. **Safari 浏览器传图失败修复**（高 #2）

#### 中优先级（5 项）
6. **待办多格式导出**（JSON / TXT / CSV）（中 #1）
7. **日记库搜索功能**（中 #2）
8. **消息记录支持天气、温度、定位**（中 #3）
9. **定时/后台自动同步**（中 #4）
10. **日记导入（多平台）**（中 #5）

#### 低优先级（4 项）
11. **读图功能**（低 #1）
12. **录音转文字功能**（低 #2）
13. **用户画像长期记忆（Beta 功能）**（低 #3）
14. **统计扩展**（低 #4）

### 技术债务
- [ ] 统一错误处理（创建 `utils/errorHandler.ts`）
- [ ] 类型定义完善（`types.ts` 添加更多 JSDoc）
- [ ] 单元测试（关键服务函数）
- [ ] E2E 测试（关键用户流程）

---

## 📅 里程碑规划

### 里程碑 1：高优先级（约 1–2 周）
- PWA（高 #1）
- Safari 浏览器传图失败修复（高 #2）

### 里程碑 2：中优先级功能扩展（约 3–4 周）
- 待办多格式导出（中 #1）
- 日记库搜索功能（中 #2）
- 消息记录支持天气、温度、定位（中 #3）
- 定时/后台自动同步（中 #4）
- 日记导入（多平台）（中 #5）

### 里程碑 3：低优先级（约 4–5 周）
- 读图功能（低 #1）
- 录音转文字功能（低 #2）
- 用户画像长期记忆（Beta 功能）（低 #3）
- 统计扩展（低 #4）

---

## 🐛 已知问题

- **Netlify WebDAV**：代理（`/api/webdav`）与 netlify.toml 已配置；若部署后仍有异常可排查转发逻辑与生产环境。
- ~~**输入框失焦问题**~~：已修复（2026-01-22）
- ~~**Safari 浏览器传图失败**~~：已提升为高优先级待修复项（高 #2）

---

**最后更新**：2026-01-22（输入框失焦问题、时间戳传递、自定义提示语、文图排版优化已完成）  
**维护者**：Wing Team
