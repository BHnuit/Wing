# Wing 功能开发与优化清单

> 基于项目文档和实际代码梳理，生成日期：2025-01-27

## 📊 功能完成度概览

### ✅ 已完成核心功能（100%）

#### 1. 记录与合成
- ✅ 碎片化记录（文本、图片 Base64）
- ✅ 消息编辑与编辑标记（`editedAt`）
- ✅ 记录页按日期展示、`?date=` 切换、日期选择器
- ✅ 日记详情「查看当天记录」跳转
- ✅ AI 智能合成（Gemini / OpenAI / DeepSeek / 自定义 Base）
- ✅ 生产环境 `/api/ai` 代理（Vercel / Netlify）
- ✅ 测试 API 连接
- ✅ 当日已有日记时「再次收拢」覆盖该日记

#### 2. 日记详情
- ✅ 手动编辑标题与 Markdown
- ✅ 编辑历史（可选）、恢复旧版本
- ✅ 重新生成（合并当日全部记录，覆盖或另存为新版本）
- ✅ 单独重新生成洞察
- ✅ 生成长图、复制到剪贴板、复制为新日记、删除
- ✅ 更多菜单聚合

#### 3. 数据与同步
- ✅ 导出 ZIP（`data.json` + `images/`）
- ✅ 导入 `.json` / `.zip`、替换
- ✅ WebDAV（坚果云、测试连接、立即同步）
- ✅ 实时同步开关（`realtimeWebdavSync`）
- ✅ 保留编辑历史、导出时是否备份 API Key 等选项

#### 4. 界面与设置
- ✅ 主题：跟随系统 / 亮色 / 暗色；黄昏与暗色（nocturnal）主题
- ✅ 猫头鹰意象：OwlLogo、EmptyStateOwl、LoadingOwl、羽毛/收拢文案
- ✅ 多语言（zh/en）、模型输出语言（与页面一致 / 中文 / 英文）
- ✅ 个人/设置页：记录热力图、今日挥动、羽毛总数
- ✅ 模型配置、显示选项、存储管理
- ✅ 欢迎日记（首次空数据注入）、错误边界与 Toast

---

## 🎯 待开发功能（按优先级）

### 🔴 高优先级（用户体验增强）

#### 1. Netlify WebDAV CORS 问题修复 ⭐⭐⭐
**状态**：待修复  
**位置**：`services/webdavService.ts`、`netlify/functions/`  
**描述**：部署到 Netlify 后 WebDAV 功能无法正常使用，报错 "CORS错误：WebDAV服务器可能不允许跨域访问"

**问题分析**：
- Netlify 静态站点无法直接代理 WebDAV 请求
- 浏览器 CORS 限制导致无法直接访问 WebDAV 服务器
- 需要创建 Netlify Function 作为代理

**实现要点**：
- 创建 `netlify/functions/webdav.ts` 作为 WebDAV 代理
- 修改 `webdavService.ts`，生产环境通过 `/api/webdav` 代理请求
- 在 `netlify.toml` 配置路由规则
- 支持所有 WebDAV 操作（GET、PUT、DELETE、PROPFIND 等）
- 错误处理和重试机制

**技术实现**：
- 使用 `node-fetch` 或 `axios` 在服务端转发请求
- 转发请求头（Authorization、Content-Type 等）
- 处理文件上传（multipart/form-data）
- 返回响应头和状态码

**预计工作量**：4-6 小时

---

#### 2. 移动端输入框优化 ⭐⭐⭐
**状态**：待优化  
**位置**：`components/ChatView.tsx`  
**描述**：移动模式下输入框过小，影响用户体验

**实现要点**：
- 增大移动端输入框高度和字体大小
- 优化移动端键盘弹出时的布局
- 支持多行输入（textarea）
- 自适应输入框高度（最多显示3-4行）
- 优化底部固定输入栏的 z-index 和定位

**技术实现**：
- 使用 Tailwind 响应式类（`md:`、`lg:`）
- 检测移动设备（`window.innerWidth < 768` 或 `navigator.userAgent`）
- 使用 `useState` 管理输入框高度
- 监听 `resize` 事件调整布局
- 使用 `viewport-fit=cover` 处理安全区域

**预计工作量**：2-3 小时

---

#### 3. 微信浏览器图片上传修复 ⭐⭐⭐
**状态**：待修复  
**位置**：`components/ChatView.tsx`（`handleImageSelect`）  
**描述**：微信浏览器内上传图片显示"图片处理失败"

**问题分析**：
- 微信浏览器对文件 API 支持可能有限制
- base64 转换可能失败
- 文件大小或格式限制

**实现要点**：
- 检测微信浏览器环境（`navigator.userAgent.includes('MicroMessenger')`）
- 优化图片压缩（使用 Canvas API 压缩大图）
- 添加更详细的错误提示
- 支持图片格式转换（HEIC 转 JPEG）
- 添加文件大小限制提示

**技术实现**：
- 使用 `canvas.toBlob()` 压缩图片
- 添加图片压缩工具函数 `utils/imageCompress.ts`
- 限制图片最大尺寸（如 2000x2000px）
- 使用 `FileReader` 的 `onerror` 捕获错误
- 提供降级方案（直接使用原图或提示用户）

**预计工作量**：3-4 小时

---

#### 4. 交互流程改造：AI 自动回复 ⭐⭐⭐
**状态**：待实现  
**位置**：`components/ChatView.tsx`  
**描述**：去除生成按钮，改为在记录页面发送消息后，AI 自动回复（由猫头鹰发送消息），并在回答内容中设置跳转链接以查看日记详情页

**实现要点**：
- 移除「收拢今日羽毛」按钮
- 用户发送消息后，自动触发 AI 合成
- AI 回复以消息形式展示（左侧，猫头鹰头像）
- 回复内容包含跳转链接（如"查看今日日记"）
- 支持手动触发重新生成（通过消息操作菜单）

**技术实现**：
- 修改 `handleSend`，发送后自动调用 `handleSynthesize`
- 创建 `AIMessage` 组件展示 AI 回复
- 使用 `react-markdown` 解析链接（`[查看日记](/journal/${entryId})`）
- 添加加载状态（显示 `LoadingOwl`）
- 错误处理：AI 失败时显示错误消息，允许重试
- 在 `types.ts` 添加 `MessageType`：`USER` | `AI`

**预计工作量**：6-8 小时

---

#### 5. 空白引导提示 ⭐⭐⭐
**状态**：未实现  
**位置**：`components/ChatView.tsx`（空状态区域）  
**描述**：聊天为空时随机展示 3–5 个引导问题，点击填入输入框

**实现要点**：
- 引导问题库（工作、生活、情感、成长等分类）
- 随机展示 3–5 个问题
- 点击问题即填入输入框
- 支持中英文双语
- 与现有 `EmptyStateOwl` 组件配合

**技术实现**：
- 在 `ChatView.tsx` 的 `fragments.length === 0` 分支添加引导问题组件
- 创建 `utils/prompts.ts` 存储问题库
- 使用 `useState` 管理随机选择的问题
- 点击时调用 `setInput(question)` 并聚焦输入框

**预计工作量**：2-3 小时

---

#### 6. 待办事项导出 ⭐⭐⭐
**状态**：未实现  
**位置**：`components/JournalDetail.tsx`（待办区域）  
**描述**：日记详情支持将待办单独导出为 JSON / TXT / CSV，或一键复制

**实现要点**：
- 导出待办按钮（在待办区域标题旁）
- 按优先级筛选（高/中/低）
- 多格式导出：JSON、TXT、CSV
- 复制到剪贴板功能
- 支持批量导出所有日记的待办

**技术实现**：
- 在 `JournalDetail.tsx` 的待办区域添加「导出待办」按钮
- 创建 `utils/todoExport.ts` 处理格式转换
- JSON：直接 `JSON.stringify(entry.todos)`
- TXT：按优先级分组，每行一个待办
- CSV：`title,priority` 格式
- 使用 `navigator.clipboard.writeText()` 实现复制

**预计工作量**：3-4 小时

---

#### 7. 文风选择 ⭐⭐
**状态**：未实现  
**位置**：设置页或合成按钮旁  
**描述**：书信体、散文体、报告体及自定义描述，写入合成时的 system prompt

**实现要点**：
- 文风选择器（设置页或合成按钮旁）
- 预设文风：书信体、散文体、报告体
- 自定义文风描述（文本输入）
- 合成时带入 system prompt
- 保存到 `AppSettings.writingStyle`

**技术实现**：
- 在 `types.ts` 添加 `writingStyle?: 'letter' | 'essay' | 'report' | 'custom'` 和 `customWritingStyle?: string`
- 在 `SettingsAiView.tsx` 或 `ChatView.tsx` 添加文风选择器
- 修改 `services/geminiService.ts` 的 `getSystemInstructionForSynthesis`，根据文风调整提示词
- 修改 `services/aiService.ts` 的 `openAICompatibleSynthesize`，同样处理文风

**预计工作量**：4-5 小时

---

### 🟡 中优先级（功能扩展）

#### 8. 分享链接 Open Graph 优化 ⭐⭐
**状态**：待实现  
**位置**：`index.html`、`public/`  
**描述**：由域名分享出去的链接无法获取到页面 icon/封面

**实现要点**：
- 添加 Open Graph meta 标签（`og:title`、`og:description`、`og:image`）
- 添加 Twitter Card meta 标签
- 创建分享封面图（1200x630px）
- 添加 favicon（多种尺寸：16x16、32x32、180x180）
- 支持动态 meta（根据日记内容生成分享卡片）

**技术实现**：
- 在 `index.html` 添加静态 meta 标签
- 创建 `public/og-image.png` 作为默认分享图
- 使用 `react-helmet` 或 `react-helmet-async` 动态更新 meta
- 在 `JournalDetail.tsx` 中根据日记内容设置动态 meta
- 生成分享卡片 API（可选）：`/api/og-image?id=${entryId}`

**预计工作量**：3-4 小时

---

#### 9. 消息记录支持天气、温度、定位 ⭐⭐
**状态**：待实现  
**位置**：`components/ChatView.tsx`、`services/weatherService.ts`  
**描述**：消息记录支持添加当天天气、温度和定位信息

**实现要点**：
- 获取用户位置（使用 Geolocation API）
- 调用天气 API（如 OpenWeatherMap、和风天气）
- 在消息记录中显示天气信息（可选，用户可开启/关闭）
- 保存位置信息到 `RawFragment.location`
- 天气信息显示在消息时间旁或消息卡片内

**技术实现**：
- 创建 `services/weatherService.ts` 封装天气 API
- 在 `types.ts` 添加 `RawFragment.weather?: { temp: number; condition: string; location?: string }`
- 在 `ChatView.tsx` 添加「添加天气」按钮或自动获取
- 使用 `navigator.geolocation.getCurrentPosition()` 获取位置
- 调用天气 API 获取当前天气
- 在消息卡片中展示天气图标和温度
- 添加设置选项：是否自动添加天气信息

**预计工作量**：5-6 小时

---

#### 10. 日记库搜索功能 ⭐⭐
**状态**：待实现  
**位置**：`components/JournalView.tsx`  
**描述**：日记库增加搜索功能，支持按标题、内容、日期搜索

**实现要点**：
- 搜索输入框（顶部或侧边栏）
- 实时搜索（输入即搜索）
- 支持关键词高亮
- 搜索结果统计
- 清空搜索按钮

**技术实现**：
- 在 `JournalView.tsx` 添加搜索输入框
- 使用 `useState` 管理搜索关键词
- 使用 `useMemo` 过滤日记列表
- 搜索逻辑：`entry.title.includes(keyword) || entry.markdownContent.includes(keyword)`
- 使用 `react-markdown` 的 `components` 属性高亮关键词
- 添加搜索历史（可选）
- 支持日期范围搜索（可选）

**预计工作量**：4-5 小时

---

#### 11. 日记导入（多平台） ⭐⭐
**状态**：未实现  
**位置**：`components/SettingsStorageView.tsx`  
**描述**：从 Day One、Journey 等导出内容导入，自动识别日期/标题、排版与命名

**实现要点**：
- 多格式支持：JSON（Day One）、TXT、MD、CSV
- 批量导入与预览
- 字段映射（日期、标题、正文、图片）
- 与现有 Wing 专用 `.json/.zip` 导入区分（新增「导入第三方日记」选项）

**技术实现**：
- 在 `SettingsStorageView.tsx` 添加「导入第三方日记」按钮
- 创建 `services/thirdPartyImport.ts` 处理不同格式解析
- Day One JSON：解析 `entries` 数组，提取 `creationDate`、`text`、`photos`
- TXT/MD：按文件名或首行识别日期，正文为内容
- CSV：按列映射（日期、标题、正文）
- 导入后转换为 `WingEntry` 格式并保存

**预计工作量**：6-8 小时

---

#### 12. 定时/后台自动同步 ⭐⭐
**状态**：未实现  
**位置**：`services/webdavService.ts`、`components/SettingsStorageView.tsx`  
**描述**：在现有「实时同步」基础上，增加定时或后台触发 WebDAV 备份

**实现要点**：
- 定时同步选项（每小时/每天/每周）
- 后台同步（使用 Service Worker 或 Web Worker）
- 同步状态提示（上次同步时间、下次同步时间）
- 失败重试机制

**技术实现**：
- 在 `AppSettings` 添加 `autoSyncInterval?: 'hourly' | 'daily' | 'weekly' | 'off'`
- 使用 `setInterval` 或 Service Worker 的 `periodicSync` API
- 在 `SettingsStorageView.tsx` 添加定时同步开关和间隔选择
- 记录上次同步时间到 localStorage
- 同步失败时记录错误并提示用户

**预计工作量**：5-6 小时

---

### 🟢 低优先级（基础设施优化）

#### 6. PWA（渐进式 Web 应用） ⭐
**状态**：未实现  
**位置**：项目根目录（`manifest.json`、Service Worker）  
**描述**：离线可用、安装到主屏

**实现要点**：
- 创建 `manifest.json`（应用名称、图标、主题色）
- 实现 Service Worker（缓存策略、离线支持）
- 安装提示（「添加到主屏」）
- 离线页面提示

**技术实现**：
- 创建 `public/manifest.json`
- 创建 `public/sw.js` Service Worker
- 在 `index.html` 注册 Service Worker
- 使用 Workbox 或手动实现缓存策略
- 在 `App.tsx` 监听 `beforeinstallprompt` 事件，显示安装提示

**预计工作量**：6-8 小时

---

#### 7. IndexedDB 迁移 ⭐
**状态**：未实现  
**位置**：`services/mockDataService.ts`  
**描述**：突破 localStorage 容量，适配大量日记与图片

**实现要点**：
- 将数据存储从 localStorage 迁移到 IndexedDB
- 保持现有 API 不变（`getEntries`、`saveEntry` 等）
- 支持数据迁移（从 localStorage 导入到 IndexedDB）
- 性能优化（批量读写、索引）

**技术实现**：
- 使用 `idb` 库或原生 IndexedDB API
- 创建数据库 schema：`entries`、`sessions`、`settings` 表
- 修改 `mockDataService.ts`，将 localStorage 操作改为 IndexedDB
- 添加迁移脚本：首次启动时检测 localStorage 数据，迁移到 IndexedDB
- 添加索引：`createdAt`、`date` 等常用查询字段

**预计工作量**：8-10 小时

---

#### 8. 统计扩展 ⭐
**状态**：未实现  
**位置**：`components/SettingsMainView.tsx`  
**描述**：心情趋势、词云、连续记录天数（Streak）等（在现有热力图与基础统计上扩展）

**实现要点**：
- 心情趋势图（按日期展示 mood emoji 变化）
- 词云（从日记正文提取高频词）
- 连续记录天数（Streak）
- 月度/年度统计

**技术实现**：
- 在 `SettingsMainView.tsx` 添加「统计详情」入口
- 创建 `components/StatsView.tsx` 展示统计
- 使用图表库（如 `recharts` 或 `chart.js`）绘制趋势图
- 使用词云库（如 `wordcloud2.js`）生成词云
- 计算 Streak：遍历 `getActivityDateSet()`，找出最长连续天数
- 统计函数：`utils/stats.ts`

**预计工作量**：8-10 小时

---

## 🔧 代码优化建议

### 1. 性能优化
- [ ] 图片懒加载（`MarkdownRenderer` 中的图片）
- [ ] 虚拟滚动（日记列表过长时）
- [ ] 防抖/节流（搜索、日期切换）
- [ ] 代码分割（按路由懒加载组件）

### 2. 用户体验优化
- [x] ~~搜索功能~~（已提升为正式功能，见「日记库搜索功能」）
- [ ] 键盘快捷键（`Cmd/Ctrl + S` 保存编辑、`Esc` 取消）
- [ ] 拖拽排序（日记列表、待办事项）
- [ ] 标签/分类（为日记添加标签）

### 3. 错误处理
- [ ] 网络错误重试机制
- [ ] 数据损坏恢复（备份检测与恢复）
- [ ] 更详细的错误提示（区分 API 错误、网络错误、解析错误）

### 4. 可访问性（A11y）
- [ ] ARIA 标签完善
- [ ] 键盘导航支持
- [ ] 屏幕阅读器优化
- [ ] 高对比度模式

---

## 📝 开发建议

### 优先级排序

#### 紧急修复（影响核心功能）
1. **Netlify WebDAV CORS 问题修复**（阻塞功能）
2. **微信浏览器图片上传修复**（移动端核心功能）
3. **移动端输入框优化**（移动端体验）

#### 核心功能改造
4. **交互流程改造：AI 自动回复**（核心交互优化）
5. **空白引导提示**（快速提升用户体验）
6. **待办事项导出**（实用功能，用户需求高）

#### 功能扩展
7. **分享链接 Open Graph 优化**（SEO/分享体验）
8. **消息记录支持天气、温度、定位**（功能扩展）
9. **日记库搜索功能**（提升查找效率）
10. **文风选择**（差异化功能）
11. **定时同步**（提升数据安全性）
12. **多平台导入**（扩大用户群体）

#### 基础设施优化
13. **PWA**（提升可用性）
14. **IndexedDB**（解决容量问题）
15. **统计扩展**（增强趣味性）

### 技术债务
- [ ] 统一错误处理（创建 `utils/errorHandler.ts`）
- [ ] 类型定义完善（`types.ts` 添加更多 JSDoc）
- [ ] 单元测试（关键服务函数）
- [ ] E2E 测试（关键用户流程）

---

## 📅 里程碑规划

### 里程碑 0：紧急修复（1 周）
- Netlify WebDAV CORS 问题修复
- 微信浏览器图片上传修复
- 移动端输入框优化

### 里程碑 1：核心交互改造（2-3 周）
- 交互流程改造：AI 自动回复
- 空白引导提示
- 待办事项导出

### 里程碑 2：功能扩展（3-4 周）
- 分享链接 Open Graph 优化
- 消息记录支持天气、温度、定位
- 日记库搜索功能
- 文风选择
- 多平台导入
- 定时同步

### 里程碑 3：基础设施优化（4-6 周）
- PWA
- IndexedDB 迁移
- 统计扩展

---

## 🐛 已知问题

### 生产环境问题
1. **Netlify WebDAV CORS 错误**：部署到 Netlify 后 WebDAV 功能无法使用，需要创建代理 Function
2. **微信浏览器图片上传失败**：需要优化图片处理逻辑，适配微信浏览器环境

### 移动端体验问题
3. **输入框过小**：移动端输入框需要增大尺寸和字体
4. **分享链接无图标**：需要添加 Open Graph meta 标签和封面图

---

**最后更新**：2025-01-27  
**维护者**：Wing Team
