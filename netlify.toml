# AI 函数 (/api/ai)：Netlify 默认 10 秒超时。若出现 504 或「请求超时 (504)」：
# - 建议：缩短当日记录、换用更快模型（如 gemini-2.0-flash）
# - Netlify Pro：可联系支持将函数超时延至 26 秒（在站点 Functions 设置或支持工单中配置）

[build]
  # 构建命令
  command = "npm run build"
  # 构建输出目录（Vite 默认输出到 dist）
  publish = "dist"
  # Node.js 版本
  node_version = "18"

[build.environment]
  # 设置 Node.js 版本
  NODE_VERSION = "18"

# AI 代理：/api/ai -> Netlify Function，须置于 /* 之前以优先生效
[[redirects]]
  from = "/api/ai"
  to = "/.netlify/functions/ai"
  status = 200

# WebDAV 代理：/api/webdav/* -> Netlify Function，在服务端转发以规避浏览器 CORS
# 使用 :splat 将路径通过 query 传入，避免 rewrite 后 event.path 在部分环境下丢失；force 避免被静态资源抢占
# 注意：请求体限制约 6MB，备份 ZIP 过大会导致 413/500；客户端已做 5.5MB 预检提示
[[redirects]]
  from = "/api/webdav/*"
  to = "/.netlify/functions/webdav?path=:splat"
  status = 200
  force = true

[[redirects]]
  from = "/api/webdav"
  to = "/.netlify/functions/webdav"
  status = 200

# 单页应用路由重写规则（因为使用了 HashRouter，这个可能不需要，但保留以防万一）
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# 安全头设置
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
